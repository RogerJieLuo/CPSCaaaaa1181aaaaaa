package week2;

/**
 * Bank account
 *
 * javadoc program extracts
 *  - the heading for your class
 *   - headings for all public methods
 *   - public instance variable
 *   - pubic static variables
 * for javadoc to extract a comment
 * the comment must precede immediately a public class definition
 * the comment must be blocked comment \/** *\/
 *
 * tags @param, @return @throws @see
 * @author
 * @version
 *
 * javadoc -d DocumentaryDirectory *.java
 *
 * some classes provide a clone method to make a new object that is identical to the implicit parameter
 *
 * attributes: private double balance; // instance variables
 *
 * java compiler initializes instance variables to 0, to false and to null for object reference.
 *
 */
public class Week2
{

}
